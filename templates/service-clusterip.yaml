apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-clusterip
  labels:
    app: {{.Release.Name}}-gitpage
spec:
  selector:
    app: {{.Release.Name}}-gitpage
  ports:
    {{- toYaml .Values.deployment.ports | nindent 4 }}
  type: ClusterIP
